dist: xenial

language: python
python:
        - '3.7'

before_script:
       - 'pip install -r requirements.txt' 
        
env: DJANGO_SETTINGS_MODULE='OCRnutella.settings'

services:
        -postgresql

script: 
- python3 manage.py test

